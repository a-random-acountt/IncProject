# Swift Package Manager (VoidCore's own standalone package build)
.build/
.swiftpm/

# Xcode - Void.xcodeproj itself IS checked in (see project.yml for the
# XcodeGen source of truth); only per-user/per-machine state is ignored.
xcuserdata/
DerivedData/

# macOS
.DS_Store
